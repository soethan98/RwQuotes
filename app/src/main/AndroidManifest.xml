<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.soethan.android.rwquotes"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <application
      android:name="com.soethan.android.rwquotes.RWQuoteApplication"
      android:allowBackup="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      tools:ignore="GoogleAppIndexingWarning">
    <activity
        android:name="com.soethan.android.rwquotes.ui.AddEditActivity"
        android:parentActivityName="com.soethan.android.rwquotes.ui.MainActivity" />
    <!-- Main Activity -->
    <activity
        android:name="com.soethan.android.rwquotes.ui.MainActivity"
        android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>